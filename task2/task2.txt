Implement duplex pipe in C using some OOP principles (see example 1).
Maximize the performance of echo test (from parent to child and back) on huge files (> 4 Gb).

Create it if needed: 
time dd if=/dev/urandom of=file.txt bs=1048576 count=1024

"Play" with buffer sizes and limits on pipe size.
